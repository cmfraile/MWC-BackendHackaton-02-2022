import Colors = require('colors.ts') ; Colors.enable();
const { argv } = require('./config/yargsconf');

console.clear();

//EMPIEZA EL CODIGO:

//let original = require('../database/original.json') ; console.log(original);
console.log(argv);

//ACABA EL CÓDIGO:
console.log("\n");